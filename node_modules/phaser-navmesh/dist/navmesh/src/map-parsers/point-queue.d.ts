import { Point } from "../common-types";
/**
 * Internal helper class to manage a queue of points when parsing a square map.
 */
export declare class PointQueue {
    private data;
    add(point: Point): void;
    shift(): Point | undefined;
    isEmpty(): boolean;
    containsPoint(point: Point): boolean;
    containsAllPoints(points: Point[]): boolean;
    getIndexOfPoint(point: Point): number;
    removePoint(point: Point): void;
    removePoints(points: Point[]): void;
}
//# sourceMappingURL=point-queue.d.ts.map